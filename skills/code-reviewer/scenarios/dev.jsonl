{"id":"quarantine-is-critical","scenario":"state.yaml records quarantine_events for T-3 (reason: test-failure, attempts: 3, last detail: 'dispatcher.test.ts timeout persisted across 3 fix attempts'). The schema is feature (not autopilot), tasks.yaml has no fix task for it, and state.yaml has no quarantine_accepted entry. scoring.critical_cap is 4. Every other dimension scores 9.","expect":["Treats the quarantined T-3 as a critical finding in the correctness dimension","Caps correctness at 4 (scoring.critical_cap) until a pending fix task exists or the user accepts the quarantine","Lists the quarantined task in the report content as T-3 with its reason (test-failure), attempts (3), and last detail","Does not pass the phase on the strength of the other dimensions"]}
{"id":"fix-tasks-minimal-and-appended","scenario":"The phase fails with two findings: the edge-case test 'escapes embedded quotes' in apps/server/src/export/csv.test.ts fails (the output drops the quote), and formatRow() in apps/server/src/export/csv.ts:41 is missing a null-guard for empty cells. tasks.yaml currently ends at T-6. While drafting fix tasks you notice the csv module would really benefit from a small refactor touching the same lines.","expect":["Emits one fix task per finding inline, each with Finding, Scope, and Approach filled in from the named test and the csv.ts:41 null-guard","Keeps fix tasks scoped to the two specific failures — no refactoring or improvements","Appends fix-1 and fix-2 sequentially with depends_on pointing at T-6 and status: pending","Returns status: failed and lets the engine route via on_failure without retry counting here"]}
