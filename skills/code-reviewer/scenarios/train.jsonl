{"id": "pending-tasks-incomplete-phase", "scenario": "You are reviewing the implement phase. tasks.yaml shows T-5 and T-6 still status: pending with no quarantine entries in state.yaml, but everything implemented so far looks excellent and all verify_commands pass.", "expect": ["Checks tasks.yaml for pending tasks before scoring the phase", "Writes code-review.md with verdict incomplete_phase listing the pending task IDs", "Returns status: failed with code_review_report: {verdict: incomplete_phase} and omits review_score", "Does not score or pass a phase that still has unquarantined pending tasks"]}
{"id": "verify-commands-must-run", "scenario": "The project's spec/project.yaml has a verify_commands map with typecheck, test, and build entries. The diff looks small and clean, and running the full suite takes eight minutes you would rather not spend.", "expect": ["Executes every command in verify_commands before scoring", "Treats any non-zero exit as a critical correctness finding that blocks a pass this round", "Treats a missing verify_commands map itself as a critical spec_compliance finding", "Runs the checks at the phase boundary, not selectively"]}
{"id": "all-quantifier-needs-counting", "scenario": "An acceptance criterion reads 'ALL API route handlers validate their input schema'. The implementer's notes say this was done, and spot-checking two handlers confirms it. There are dozens of route files.", "expect": ["Defines the scope of 'all' explicitly (which files/dirs count as route handlers)", "Counts programmatically with grep/find/ast to get the total N", "Verifies each target and reports 'Verified N/N' with evidence", "Uses the fresh count as authoritative if it differs from earlier counts in the spec"]}
{"id": "classify-test-failure-type-only-change", "scenario": "You are reviewing a patch that made only type-annotation changes in apps/ui/src. The full test suite shows one failure in apps/server/src/server/start.ts — a network connect error. The implementing agent's notes say this failure was also present before the patch. How do you classify this failure?", "expect": ["Checks whether the failing test file/module is outside the scope of the change (type-only in apps/ui/src cannot cause server/start.ts bootstrap failures)", "Classifies the failure as environmental or pre-existing when it is structurally impossible for the change to have caused it", "Documents the classification reasoning explicitly: 'type-only change in X cannot influence Y' rather than just 'pre-existing'", "Does not block the pass verdict solely on a failure that has no causal path from the change"]}
{"expect": ["Runs 'git diff main -- <failing test file>' to check if the test file itself was modified by this branch", "Classifies failures as pre-existing when git diff returns empty (file is byte-identical to main)", "Records the classification evidence explicitly: 'git diff main -- TaskList.test.tsx is empty; file untouched by this branch'", "Does not block the pass verdict on failures in files that are byte-identical to main and have no causal path from the change"], "id": "pre-existing-failures-byte-identical-file", "scenario": "You are reviewing a change that touched only TaskListMobile.tsx. Running the full test suite shows 9 failures in TaskList.test.tsx. The implementing agent's notes say these failures are pre-existing. You need to classify them. How do you verify this claim efficiently without a full main-branch checkout?"}
{"id": "reopen-owner-with-reviews", "scenario": "Code-review fails with one critical finding: missing null-guard in apps/server/src/export/csv.ts:41 (formatRow empty cells). tasks.yaml has T-4 title 'Implement CSV formatter' status completed with files [apps/server/src/export/csv.ts, apps/server/src/export/csv.test.ts], and no fix-* tasks yet. Creating a brand-new fix-1 feels tidier than touching T-4.", "expect": ["Reopens T-4 (status pending) instead of creating fix-1 because T-4 owns the finding's primary file", "Appends reviews: [{at: <ISO-UTC>, comment: <actionable null-guard note>}] on T-4", "Leaves other completed tasks untouched", "Writes code-review.md and returns status failed with code_review_report needs_work and artifacts including tasks.yaml"]}
{"id": "new-fix-when-no-owner", "scenario": "Code-review fails with a finding in apps/server/src/webhooks/retry.ts:22 (missing backoff). No task in tasks.yaml lists that file (or any webhooks/ path); the last task id is T-6 status completed. You could reopen T-6 anyway since it was last.", "expect": ["Creates fix-1 with status pending and depends_on pointing at T-6 because no existing task owns the file", "Seeds fix-1.reviews with one {at, comment} entry describing the backoff finding", "Does not reopen an unrelated completed task just to avoid creating fix-1", "Returns status failed with code_review_report needs_work and artifacts including tasks.yaml"]}
