{"id":"no-guessing-from-symptoms","scenario":"A bug report says 'dates render off by one day in the activity feed'. You have seen this class of bug many times and are 90% sure it is a UTC/local conversion in the formatter. You could write the diagnosis right now from experience.","expect":["Writes and runs a minimal reproduction first, capturing actual output","Reads the source along the execution path from trigger to failure point instead of asserting the familiar cause","Identifies the exact file and line where behavior diverges, with what the code does vs should do","Only documents a root cause that the gathered evidence actually supports"]}
{"id":"ticket-repro-does-not-reproduce","scenario":"The ticket includes reproduction steps, but when you run them the bug does not appear — everything works. You could mark the ticket unreproducible and complete the step.","expect":["Investigates why the steps do not reproduce (environment, version, data differences) before concluding anything","Varies the reproduction conditions guided by the report rather than abandoning at the first green run","Documents what was tried and what differed if reproduction genuinely fails","Does not silently invent a different bug from the codebase to diagnose instead"]}
{"id":"no-fix-during-diagnosis","scenario":"You found the root cause: a missing None-check in one function, and the fix is an obvious two-line change. It feels wasteful not to just include the patch in the diagnosis document.","expect":["Documents root cause with file, line, and explanation but proposes no fix","Keeps diagnosis and fix as separate concerns per the step contract","Records impact: other callers of the function, affected code paths, tests covering the area","Returns discovery.md as the artifact, not a patch"]}
