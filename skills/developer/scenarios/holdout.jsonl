{"id":"dev-commit-failure-stops-completion","scenario":"All verify commands for T-2 pass and you stage only apps/server/src/webhooks/dispatcher.ts and apps/server/src/webhooks/dispatcher.test.ts. The git commit command is rejected by the environment (pre-commit hook exits 1 with a husky permission error). The code changes remain in the worktree. What completion result do you return?","expect":["Does not mark the task completed in tasks.yaml or claim COMPLETION status completed without a commit confirmed in git log","Records the commit failure (hook exit 1) in known_concerns","Stops implementation instead of moving to later tasks or marking T-2 completed in tasks.yaml","Distinguishes verified worktree changes from a completed task"]}
{"id":"dev-status-after-commit","scenario":"You have implemented T-3 and its verify commands pass. To save time you consider setting tasks.yaml status: completed for T-3 before creating the commit, then committing the code and status change together. What do you do?","expect":["Commits the verified task before recording it as completed in tasks.yaml","Updates T-3's status immediately after the commit rather than batching status updates","Records the requested task metadata with the completion update","Does not let a pre-commit status claim substitute for a confirmed commit"]}
{"id":"dev-update-stale-docstring","scenario":"A task removes the deprecated include_archived parameter from listTasks() in apps/server/src/tasks/list.ts. Tests pass after the code change, but a grep of the same file finds a docstring that still tells callers to pass include_archived: true. What do you do?","expect":["Updates the stale docstring atomically with the removed parameter","Names a targeted search (e.g. grep for include_archived across the repo) to find other comments or docs referring to the removed parameter","Does not dismiss stale developer-facing documentation because the tests pass","Re-runs appropriate verification after the documentation correction"]}
