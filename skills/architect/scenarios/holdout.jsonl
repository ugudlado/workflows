{"id": "completion-outputs-complete", "scenario": "Feature BKG-608 (webhook dispatcher on task status changes): design.md and tasks.yaml are written and validated — design.md at $WORKTREE_ARTIFACT_DIR/BKG-608/design.md describes a transactional-outbox table plus a polling dispatcher job with Selected Approach 'transactional outbox with polling dispatcher' (complexity M); tasks.yaml holds 5 tasks; discovery.md Key Decisions records the same selection. You are about to return COMPLETION and are tempted to also emit design_direction / complexity / updated_artifact_set handles because older prompts required them.", "expect": ["Emits COMPLETION with status: completed, outputs.reason, and artifacts: [design.md, tasks.yaml] — no design_direction, complexity, updated_artifact_set, or path-keyed outputs", "Confirms Selected Approach in design.md already carries the approach name and complexity M", "Confirms discovery.md Key Decisions already records the chosen direction", "Emits an APPROACH block before the artifact content since this step produces multi-file artifacts"]}
{"id": "bugfix-order-regression-first", "scenario": "flags.bugfix is true for BKG-612: archiving a task crashes with a null-pointer when the task has no assignee — apps/server/src/tasks/archive.ts:88 dereferences task.assignee.id without a guard. The repo's tests run with bun test. Task breakdown feels natural as: T-1 fix the crash, T-2 add a regression test, since the fix is a one-line guard and well understood.", "expect": ["Orders tasks with the regression test first (T-1) and the fix second (T-2)", "Emits the filled-in task content inline for this scenario: real task ids, the file apps/server/src/tasks/archive.ts, and concrete verify commands (bun test on the regression test file) — not placeholder templates", "Ensures T-1's verify accounts for the expected-fail state before the fix lands", "Covers the no-assignee acceptance criterion with a testable command or assertion"]}
