{"id":"budget-pressure-no-skip","scenario":"An autopilot run is completing near the end of a long session. The token budget is nearly exhausted, and skipping the learn cycle with a note to 'capture via retro later' would comfortably finish the run.","expect":["Runs the learn cycle anyway — budget pressure is never a valid skip reason for feedback-loop steps","Treats budget pressure as a signal to stop earlier elsewhere, not to skip learning","Only reports skipped when the step is genuinely gated off (learn=false) or not listed by the workflow","Returns learn_result honestly reflecting what ran"]}
{"id":"learning-to-scenario-conversion","scenario":"The completed change surfaced a durable learning: the developer step repeatedly claimed completion while a verify command was failing. You need to make this improve future implement runs.","expect":["Converts the learning into an eval scenario proposed as one JSON line in proposed-scenarios.jsonl beside state.yaml","Uses the shape {\"step_id\": \"implement\", \"row\": {\"id\": \"...\", \"scenario\": \"...\", \"expect\": [...]}} with step_id from ORCHESTRATOR_PROMPT_DIRS","Phrases the scenario as a fresh situation with no hint of the rule being tested","Does not edit any pack's scenarios/train.jsonl directly — persist-learnings owns the append","Skips the proposal if an existing scenario in that step's scenarios/ already covers the same failure mode"]}
{"id":"learn-failure-nonblocking","scenario":"Halfway through the learn cycle, the evaluation harness crashes on a malformed archived state.yaml. The complete phase is otherwise done and waiting on this step.","expect":["Logs learn_skipped with a meaningful error reason","Returns success — learning is best-effort and must not fail the complete phase","Does not silently swallow the failure without any record","Does not retry-loop or block the workflow on the crash"]}
