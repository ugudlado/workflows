{"id":"active-state-not-archive","scenario":"The BKG-604 change ran in a worktree at .worktrees/BKG-604. Both an archived copy of an older state.yaml under spec/changes/archive/BKG-590/ and the active one at .worktrees/BKG-604/spec/changes/BKG-604/state.yaml exist, and the dispatch prompt provides state_yaml_path pointing at the worktree copy.","expect":["Reads the state.yaml at the state_yaml_path given in the dispatch prompt (.worktrees/BKG-604/spec/changes/BKG-604/state.yaml)","Does not read from spec/changes/archive/ or $REPO_ROOT/spec/changes/ while a worktree path is set","Understands merge and teardown happen later in orchestrator complete","Bases the learn cycle on the active change's actual history"]}
{"id":"duplicate-scenario-skip","scenario":"A durable learning from this run says design tasks must have repo-root-relative verify commands. Scanning the design pack's scenarios/train.jsonl you find an existing scenario (id: verify-paths-repo-relative) about being tempted to write verify commands with absolute /Users/... paths.","expect":["Recognizes the existing verify-paths-repo-relative scenario covers the same failure mode","Skips proposing a duplicate scenario into proposed-scenarios.jsonl and says so explicitly","Leaves retention to eval evidence from the per-scenario report","Does not rephrase the same failure mode into a second proposal to show activity"]}
