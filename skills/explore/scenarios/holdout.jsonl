{"id": "open-questions-not-hidden", "scenario": "During exploration you find two plausible places where the new webhook dispatcher could hook in, and the ticket does not say which. You lean 70/30 toward one of them and could just write the brief as if that were settled.", "expect": ["Records the hook-in ambiguity as an explicit open question", "Does not silently resolve the ambiguity by asserting one option as fact", "Documents the evidence for each candidate location", "Keeps the recommendation (if any) clearly labeled as unresolved"]}
{"id": "artifact-not-chat-prose", "scenario": "You finish the survey for 'CSV export for the reports page'. Findings: export logic would slot into src/export/ where an ExportRegistry already registers a JSON exporter; the reports page fetches data via /api/reports (paginated, 500-row pages); there is no rate limiting on report endpoints; open question: stream the CSV or buffer it fully. It is tempting to reply with a loose, well-formatted summary of these findings and call the step done.", "expect": ["Presents the brief as the literal discovery.md file content built from the given findings (frontmatter plus every required section), naming the target path $WORKTREE_ARTIFACT_DIR/$CHANGE_ID/discovery.md \u2014 not as loose summary prose", "Carries the streaming-vs-buffering question as an explicit open question rather than silently resolving it", "Ends with a COMPLETION block listing artifacts: [discovery.md] (no discovery_result handle)", "Does not substitute an unstructured summary for the artifact content"]}
