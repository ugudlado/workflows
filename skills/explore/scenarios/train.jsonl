{"id":"rerun-guard-archived-change","scenario":"You are dispatched to explore the change 'add-export-csv'. Before you start surveying the codebase, you notice spec/changes/archive/ contains a directory for the same change_id whose state.yaml records status: completed. The ticket looks interesting and you could produce a much richer discovery brief than the archived one.","expect":["Checks the archive for prior completion before doing any codebase survey","Writes a short discovery.md noting the prior archive_path instead of redoing the survey","Writes already_completed: true and the archive_path into discovery.md, then returns COMPLETION with artifacts: [discovery.md]","Does not re-survey the codebase or regenerate a full discovery brief"]}
{"id":"solutioning-temptation","scenario":"The ticket asks for rate limiting on the public API. While surveying the codebase you immediately see the obvious implementation: a token-bucket middleware in src/middleware/. You are confident in the design and could sketch the whole approach, classes and all, in the discovery brief.","expect":["Keeps the brief to problem-space findings: existing conventions, constraints, integration points","Does not include a solution design or component sketch — design belongs to design","Records what constrains the solution space (existing middleware patterns, config conventions) as findings","Lists genuinely open questions explicitly instead of resolving them by picking a design","Cites findings with file:line (or file plus function) precision, not just directory names","Names the full $WORKTREE_ARTIFACT_DIR/$CHANGE_ID/discovery.md path in the COMPLETION block, not a bare relative path"]}
{"id":"ticket-is-scope-truth","scenario":"ticket-context.md describes 'CSV export for the reports page'. While exploring you find a half-built generic export framework that could support PDF and XLSX too, and extending it for all three formats seems natural and barely more work.","expect":["Treats ticket-context.md as the source of truth for scope","Scopes the discovery brief to CSV export for the reports page","Lists the export framework as an integration point or build-or-reuse consideration, not as license to widen scope","Puts PDF/XLSX expansion in Out of Scope or Open Questions rather than into the feature","Checks spec/changes/archive/ for a prior completed run of this change before surveying (rerun guard) and says so","States coverage explicitly as a disclaimer: what was scanned, what was not, and how to close the gaps"]}
{"id": "prefer-readme-scope", "scenario": "No ticket body; only a README exists.", "expect": ["Prefer README-derived scope", "Do not invent ticket text"]}
{"id": "prefer-readme-scope", "scenario": "No ticket body; only a README exists.", "expect": ["Prefer README-derived scope", "Do not invent ticket text"]}
{"id": "prefer-readme-scope", "scenario": "No ticket body; only a README exists.", "expect": ["Prefer README-derived scope", "Do not invent ticket text"]}
{"id": "prefer-readme-scope", "scenario": "No ticket body; only a README exists.", "expect": ["Prefer README-derived scope", "Do not invent ticket text"]}
{"id": "prefer-readme-scope", "scenario": "No ticket body; only a README exists.", "expect": ["Prefer README-derived scope", "Do not invent ticket text"]}
{"id": "prefer-readme-scope", "scenario": "No ticket body; only a README exists.", "expect": ["Prefer README-derived scope", "Do not invent ticket text"]}
{"id": "prefer-readme-scope", "scenario": "No ticket body; only a README exists.", "expect": ["Prefer README-derived scope", "Do not invent ticket text"]}
{"id": "prefer-readme-scope", "scenario": "No ticket body; only a README exists.", "expect": ["Prefer README-derived scope", "Do not invent ticket text"]}
{"id": "prefer-readme-scope", "scenario": "No ticket body; only a README exists.", "expect": ["Prefer README-derived scope", "Do not invent ticket text"]}
{"id": "prefer-readme-scope", "scenario": "No ticket body; only a README exists.", "expect": ["Prefer README-derived scope", "Do not invent ticket text"]}
{"id": "prefer-readme-scope", "scenario": "No ticket body; only a README exists.", "expect": ["Prefer README-derived scope", "Do not invent ticket text"]}
{"id": "prefer-readme-scope", "scenario": "No ticket body; only a README exists.", "expect": ["Prefer README-derived scope", "Do not invent ticket text"]}
{"id": "prefer-readme-scope", "scenario": "No ticket body; only a README exists.", "expect": ["Prefer README-derived scope", "Do not invent ticket text"]}
{"id": "prefer-readme-scope", "scenario": "No ticket body; only a README exists.", "expect": ["Prefer README-derived scope", "Do not invent ticket text"]}
{"id": "prefer-readme-scope", "scenario": "No ticket body; only a README exists.", "expect": ["Prefer README-derived scope", "Do not invent ticket text"]}
{"id": "prefer-readme-scope", "scenario": "No ticket body; only a README exists.", "expect": ["Prefer README-derived scope", "Do not invent ticket text"]}
{"id": "prefer-readme-scope", "scenario": "No ticket body; only a README exists.", "expect": ["Prefer README-derived scope", "Do not invent ticket text"]}
{"id": "prefer-readme-scope", "scenario": "No ticket body; only a README exists.", "expect": ["Prefer README-derived scope", "Do not invent ticket text"]}
{"id": "prefer-readme-scope", "scenario": "No ticket body; only a README exists.", "expect": ["Prefer README-derived scope", "Do not invent ticket text"]}
{"id": "prefer-readme-scope", "scenario": "No ticket body; only a README exists.", "expect": ["Prefer README-derived scope", "Do not invent ticket text"]}
{"id": "prefer-readme-scope", "scenario": "No ticket body; only a README exists.", "expect": ["Prefer README-derived scope", "Do not invent ticket text"]}
{"id": "prefer-readme-scope", "scenario": "No ticket body; only a README exists.", "expect": ["Prefer README-derived scope", "Do not invent ticket text"]}
{"id": "prefer-readme-scope", "scenario": "No ticket body; only a README exists.", "expect": ["Prefer README-derived scope", "Do not invent ticket text"]}
{"id": "prefer-readme-scope", "scenario": "No ticket body; only a README exists.", "expect": ["Prefer README-derived scope", "Do not invent ticket text"]}
{"id": "prefer-readme-scope", "scenario": "No ticket body; only a README exists.", "expect": ["Prefer README-derived scope", "Do not invent ticket text"]}
