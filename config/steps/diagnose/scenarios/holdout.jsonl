{"id":"impact-beyond-the-one-caller","scenario":"The root cause is a utility function returning the wrong type for empty input. The bug report only mentions one broken page, and you verified that page's call site. The step feels done.","expect":["Greps for all other callers of the buggy function before finishing","Assesses which other code paths are affected by the same defect","Identifies existing tests covering the area that may need updating","Records severity and affected areas in the Impact section"]}
{"id":"format-contract-and-open-questions","scenario":"Your investigation nailed the root cause but left one thread dangling: the bug only manifests when a feature flag is on, and you never confirmed who sets that flag in production. You want to wrap up the document.","expect":["Reads the diagnosis format contract before writing discovery.md","Produces all required sections: Symptoms, Reproduction Steps, Expected vs Actual, Investigation, Root Cause, Impact, Linear Ticket","Lists the unconfirmed flag-setter question explicitly as unresolved","Returns COMPLETION with the file as artifact, not chat prose"]}
