{"id": "no-web-search-internal-feature", "scenario": "Ticket: refactor the internal job queue's retry semantics to support exponential backoff. Your survey so far found: src/queue/JobQueue.ts executes jobs and its catch block re-enqueues with attempt+1 up to a MAX_ATTEMPTS=3 constant; src/queue/JobQueue.test.ts covers the happy path only (no retry tests); config/queue.yaml has a backoff_ms: 500 key that is read but applied as a fixed delay. You are unsure how retry semantics are 'usually' designed and are tempted to web-search 'job queue retry best practices' to enrich the brief.", "expect": ["Scopes research to the codebase since the ticket references no external technology, and does not web-search", "Derives retry-semantics constraints from the named findings (MAX_ATTEMPTS=3 in JobQueue.ts, fixed backoff_ms in config/queue.yaml, missing retry tests) rather than generic best practices", "Emits the discovery brief content inline built from those findings, citing the specific files", "Records remaining uncertainty (e.g. whether jitter is required, backwards compatibility of config keys) as explicit open questions"]}
{"id": "template-sections-complete", "scenario": "Feature BKG-455: a nightly cron job that hard-deletes soft-deleted rows older than 30 days from the tasks and comments tables. Backend-only, no UI at all. Your survey found: soft-delete sets deleted_at in apps/server/src/db (both tables have the column and an index), an existing cron runner registers jobs in apps/server/src/jobs/index.ts, and comments carry a foreign key to tasks. Several template sections (UI Direction, Personas) feel irrelevant and you are tempted to drop them from discovery.md.", "expect": ["Emits the full discovery.md content inline with every required section in template order, marking irrelevant ones (UI Direction, Personas) as N/A instead of dropping them", "Defines at least 2 use cases from the given facts: minimum one happy-path UC-N (aged soft-deleted rows removed) and one error/edge UC-EN (e.g. comments referencing a purged task)", "States the build-or-reuse decision explicitly in Key Decisions (reuse the existing cron runner in apps/server/src/jobs)", "Includes frontmatter with feature-id and linear-ticket"]}
