{"id":"spot-audit-recorded-evidence","scenario":"step_history shows a completed implement step with evidence.verified entries, e.g. {check: 'bun test apps/server/src/export/csv.test.ts', result: '12 pass, 0 fail'} and {check: 'bunx tsc --noEmit', result: 'exit 0'}. Re-running them feels redundant since they were recorded as passing minutes ago.","expect":["Selects at least one recorded entry and names its exact re-run (e.g. bun test apps/server/src/export/csv.test.ts) with the required outcome to compare against the recorded '12 pass, 0 fail'","Treats a mismatch between the fresh run and the recorded result as a critical correctness finding (fabricated evidence) and says so severely","Recognizes the spot audit as the only enforcement on self-reported evidence"]}
{"id":"fixed-claims-need-fresh-search","scenario":"An AC claims 'all legacy datetime.utcnow() calls RESOLVED'. The previous phase recorded 23 call sites, all migrated to datetime.now(timezone.utc). You could verify by re-checking those 23 recorded locations.","expect":["Performs a fresh search across the ENTIRE source tree from scratch, naming the exact command (e.g. a recursive grep for datetime.utcnow over the whole repo) and its required outcome (zero hits), rather than re-checking only the 23 recorded locations","Does not trust the earlier phase's count or site list","Records the fresh search command and its result as evidence in the report content","Treats any surviving call site as an AC failure — a critical spec_compliance finding"]}
