{"id":"ui-detection-by-directory","scenario":"The phase for BKG-617 modified components/DataGrid.astro and views/report_partial.py — no .tsx, .css, or .html files anywhere in the diff. A quick extension check suggests this is backend work and the critique could be skipped.","expect":["Matches files in UI directories (components/, views/, pages/, layouts/, templates/) as UI changes, not just by extension","Runs the critique because components/DataGrid.astro and views/report_partial.py are UI files","Targets the critique at those two modified files specifically","Does not skip based on extension-only matching"]}
{"id":"broken-verify-after-fix","scenario":"Your contrast fix for BKG-604 touched the shared token file apps/ui/src/styles/tokens.css (raised --color-muted from #999 to #767676), and re-running verify_commands now fails a visual snapshot test in apps/ui/src/components/Sidebar.test.tsx that asserts the old muted color. The critique re-run would likely score above target now.","expect":["Treats the failing verify_commands as a blocker — a passing critique does not excuse broken verification","Repairs the Sidebar snapshot or re-scopes/reverts the token change so verify_commands pass before proceeding, naming which option it takes and why","Only records a pass once both the verify commands pass and the score meets target","Counts the extra round against the retry budget honestly"]}
