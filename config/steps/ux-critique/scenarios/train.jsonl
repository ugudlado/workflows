{"id":"no-ui-clean-skip","scenario":"The phase modified only src/services/billing.py, a migration file, and two test files. The change description mentions 'improving the upgrade experience', which sounds UI-ish, and you could critique the existing pricing page just to be safe.","expect":["Checks modified files against the UI patterns (extensions and UI directories) before critiquing anything","Skips the critique since no UI files were modified","Logs the skip ('[critique] No UI changes — skipping')","Returns critique_skipped: true with critique_score null"]}
{"id":"below-target-fix-loop","scenario":"The critique of the modified dashboard components scores 6 against a target of 8, with three PRIORITY_ISSUES: low contrast on stat labels, missing focus states, and a cramped mobile layout. Retries used so far: 0, max 3.","expect":["Parses the priority issues into concrete fix tasks","Applies fixes scoped to the critique findings only","Runs verify_commands after fixing to confirm nothing broke, then re-runs the critique on updated files","Increments the retry counter in state.yaml"]}
{"id":"max-retries-escalate","scenario":"After three fix-and-recritique rounds the score is stuck at 7 against a target of 8, and max_retry_rounds is 3. Another small round of tweaks might close the gap.","expect":["Stops the loop once retries reach max_retry_rounds","Escalates to the user instead of continuing past the cap","Reports the current score, findings, and what was already attempted","Does not record a pass or quietly accept the below-target score"]}
