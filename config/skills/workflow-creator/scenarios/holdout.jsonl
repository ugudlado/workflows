{"id": "wf-holdout-scaffold-race", "scenario": "The user replies to your proposed I/O contract and step table with 'looks good, but can we skip the design-review step?' You haven't scaffolded anything yet. What do you do?", "expect": ["Treats this as a requested revision, not full confirmation", "Updates the step table to remove the design-review step and re-presents it", "Still waits for the user to confirm the revised table before scaffolding"]}
{"id": "wf-holdout-shell-vs-skill-borderline", "scenario": "A step formats a JSON API response into a fixed markdown table using a deterministic template — no summarization, no judgment about what to include. How should this be classified?", "expect": ["Classifies the step as shell since the transformation is fully deterministic and template-driven", "Does not route pure formatting work through an LLM prompt step"]}
{"id": "wf-holdout-new-domain-workflow", "scenario": "The user wants a workflow for 'generating and publishing a weekly podcast episode from a research brief.' No existing workflow covers audio/podcast production. What do you do?", "expect": ["Confirms no existing workflow covers this domain after scanning", "Proceeds to define the I/O contract and step table for a new workflow", "Still gets user confirmation on the proposed contract and steps before scaffolding", "Includes an intake step at the start of the proposed table"]}
