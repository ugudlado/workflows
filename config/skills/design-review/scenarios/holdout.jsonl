{"id":"findings-not-fixes","scenario":"Reviewing BKG-608: tasks.yaml has an obvious one-line defect — T-2's verify command reads 'bun test apps/server/src/webhoks/dispatcher.test.ts' (typo'd path; the directory is webhooks/). Fixing it yourself and passing the review would save a whole architect round-trip.","expect":["Does not edit design.md or tasks.yaml — findings only","Reports the typo as a finding naming T-2 and the exact broken path apps/server/src/webhoks/dispatcher.test.ts","Returns needs_work via status: failed so the architect addresses it","Leaves retry counting to the engine's max_retries"]}
{"id":"severity-caps-apply","scenario":"Reviewing BKG-613: completeness is clean (9), feasibility has one important finding (the retry-backoff open question left unresolved), and task_quality has one critical finding (T-4 has no verify command at all). project.yaml sets quality_bar.min_design_review_score: 8. You are computing the dimension scores and the verdict.","expect":["Caps the critical dimension (task_quality) at 4 and the important-finding dimension (feasibility) at 7","Computes overall as the minimum across all five dimensions — 4 here","Compares overall 4 against the quality_bar.min_design_review_score of 8 for the verdict","States that the critical finding forces needs_work regardless of overall"]}
