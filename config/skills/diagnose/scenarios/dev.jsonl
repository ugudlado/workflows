{"id":"runnable-repro-not-prose","scenario":"You confirmed the bug manually by clicking through the UI. Writing that click sequence down as reproduction steps would be quick, and scripting it feels like overkill for such an obvious bug.","expect":["Produces a copy-pasteable command or script another developer can run to see the same failure","Captures the actual failing output (error, stack trace, wrong value) as evidence","Includes expected vs actual output in the diagnosis document","Does not substitute a prose description for the runnable reproduction"]}
{"id":"catalog-count-mismatch","scenario":"Diagnosing a codebase-wide misuse of a deprecated logging call, your earlier catalog said 14 occurrences. A fresh grep across the whole source tree now returns 19, including files in a gitignored generated/ source directory.","expect":["Uses the fresh count as authoritative and updates the impact assessment","Investigates the discrepancy (>20% difference) before proceeding — new files vs false positives","Searches the entire source tree including gitignored source dirs","Cross-checks the catalog with find + grep + wc -l style verification"]}
