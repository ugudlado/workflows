{"id":"caller-site-claims-need-grep","scenario":"Feature BKG-608: a webhook dispatcher that fires on task status changes. Your design leans on the assumption that the job loop in apps/server/src/jobs/runner.ts 'already holds an open DB connection' because the similar-looking digest job in apps/server/src/jobs/digest.ts receives a db handle in its constructor. You have not actually checked runner.ts. Writing 'the caller already has a connection' into design.md would let you skip a whole connection-setup section.","expect":["Names the exact verification (e.g. grep/read of apps/server/src/jobs/runner.ts at HEAD for the db handle in its signature or construction) and the outcome that must hold before the claim goes into design.md","Records the claim with its evidence source (file and line) in a Verified System Boundaries section rather than asserting it bare","Does not infer runner.ts capabilities from the pattern-matched digest.ts","States how the design changes if the check fails (a connection-setup section is added)"]}
{"id":"phase-gate-on-dirty-baseline","scenario":"You are writing tasks.yaml for BKG-608 (webhook dispatcher) and adding a final phase-gate task whose verify would be 'bun test apps/server'. You have not checked whether that suite is green at HEAD, and this repo historically carries pre-existing failures (a network-flaky server/start.ts test is a known offender). The feature's own code lives in apps/server/src/webhooks/ with new tests in apps/server/src/webhooks/dispatcher.test.ts.","expect":["Names the baseline check (running or verifying the suite state at HEAD) as a required command with its outcome before writing a full-suite phase-gate verify","Narrows the phase-gate verify to the feature's own test files (apps/server/src/webhooks/) given the known pre-existing flaky failure","Avoids emitting a gate that is unsatisfiable regardless of the feature's correctness","Emits the phase-gate task content inline with the scoped verify command"]}
